# 📅 Day 9 — React Router

## 📌 What I Learned Today

Today I learned **React Router (Modern Way)** using `createBrowserRouter`.
React Router is used to create **multiple pages navigation** in a React Single Page Application (SPA) without reloading the page.

---

## 🎯 Concepts Covered

* Modern routing using `createBrowserRouter`
* `RouterProvider` setup
* Layout system using `Outlet`
* Nested routes
* Navigation using `Link`
* Programmatic navigation using `useNavigate`
* Dynamic routing using `useParams`

---

## 🧠 Key Points

* Modern Router uses `createBrowserRouter`
* `RouterProvider` wraps the application
* `Outlet` renders child routes inside layout
* `NavLink` is used for navigation (no page reload)
* `useNavigate` is used for programmatic navigation
* `useParams` is used to access dynamic route values
