# 📅 Day 10 — React Hook Form

## 📌 What I Learned Today

Today I learned **React Hook Form** in React.
React Hook Form is a lightweight library used to manage forms efficiently with better performance and less re-rendering.

---

## 🎯 Concepts Covered

* Use `useForm()` hook
* Registering inputs using `register`
* Handling form submission with `handleSubmit`
* Accessing validation errors using `formState.errors`
* Applying validation rules (required, pattern, minLength)

---

## 🧠 Key Points

* React Hook Form reduces unnecessary re-renders
* No need to manage input state manually using `useState`
* Built-in validation support
* Better performance for large forms
