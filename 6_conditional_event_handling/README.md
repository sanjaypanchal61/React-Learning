# 📅 Day 6 - Conditional Rendering & Event Handling  

## 🚀 Topics Learned

### 1. Conditional Rendering

* Learned how to show different UI based on conditions
* Used |**ternary operator**
* Example: Login / Logout UI

### 2. Event Handling

* Learned how to handle user actions in React
* Learned difference between:

  * Function reference → `onClick={handleClick}`
  * Function call (wrong) → `onClick={handleClick()}`

---

## 🧠 Key Concepts

* JSX में direct `if` use नहीं कर सकते
* Ternary operator most commonly used
* Event names use camelCase (onClick, onChange)
* Always pass function reference, not function call